<table>
    <thead>
        <tr>
            <th colspan='6'>Cat</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td><%= @cat.name %> </td>
            <td><%= @cat.age %></td>
            <td><%= @cat.color %>
            <td><%= @cat.birth_date %></td>
            <td><%= @cat.sex %></td>
            <td><%= @cat.description %></td>
        </tr>
     </tbody>
</table>